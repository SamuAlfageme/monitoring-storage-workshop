apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-unbound
  namespace: default
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-storage
  resources:
    requests:
      storage: 10Gi
